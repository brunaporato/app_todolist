import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    // width: '100%',
  },

  header: {
    backgroundColor: '#0D0D0D',
    padding: 70,
    alignItems: 'center',
    paddingTop: 90,
  },

  logo: {
    width: 110,
    height: 32,
  },
})
